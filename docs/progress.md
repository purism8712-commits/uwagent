2026-06-15 STEP 1 완료 - 기준 문서(PRD, customer, architecture, AGENTS) 검토 후 목표·핵심기능·완료정의·안전규칙 요약 기준 확정.
2026-06-15 STEP 2 완료 - 신계약가이드라인 입력 템플릿 구조(Rule Master, Note Master, Rule-Note Map, Change Log)가 공통 코어 에이전트 입력 형식으로 적합함을 확인.
2026-06-15 STEP 3 완료 - 루트 AGENTS.md를 원본 기준표를 구조화 입력 템플릿으로 변환하는 공통 코어 에이전트 전용 작업 원칙 문서로 재작성.
2026-06-15 STEP 4 완료 - docs/prd.md, customer.md, architecture.md, AGENTS.md를 공통 코어 에이전트 기준으로 재작성하여 문서 세트를 정합화.
2026-06-15 STEP 5 완료 - 신계약가이드라인.xlsx를 기반으로 Rule Master, Note Master, Rule-Note Map, Change Log 4개 시트의 공통 코어 샘플 결과 워크북 생성 및 검증.
2026-06-15 STEP 6 완료 - 소액암진단 단일건 한도 1000→2000 변경 시나리오를 반영한 공통 코어 샘플 결과 v2 워크북 생성 및 변경 이력 검증.
2026-06-15 STEP 7 완료 - docs/AGENTS.md에 검토메모를 사용자 확인 질문으로 전환하는 규칙과 질문 생성 원칙을 추가.
2026-06-15 STEP 8 완료 - 공통 코어 샘플 v2를 바탕으로 내부 현장 담당자용 심사 에이전트 예시 초안(공지 + 핵심 FAQ) 문서 작성.
2026-06-15 STEP 9 완료 - BizRouter 연동용 .env 템플릿과 민감정보 업로드 방지용 .gitignore를 루트에 생성.
2026-06-15 STEP 10 완료 - BizRouter LLM 연동용 base URL, 모델명, API 키를 루트 .env에 반영.
2026-06-15 STEP 11 완료 - Next.js 기반 공통 코어 앱 UI를 구현하고 입력 화면, 검토메모/질문 화면 전환 테스트 및 빌드 검증을 완료.
2026-06-15 STEP 12 완료 - 초안확정 후 변경 최종 요약 표시 및 공통 코어 양식 엑셀 다운로드 API/화면 연동 구현과 테스트·빌드·API 검증 완료.
2026-06-15 STEP 13 완료 - Next.js 개발 서버 청크 캐시 오류(Cannot find module './331.js')를 .next 초기화 및 서버 재기동으로 복구하고 루트 페이지 200 응답을 확인.
2026-06-15 STEP 14 완료 - draft-summary/draft-export API가 레거시 요청 형식(questionAnswers, uploadedFileName, userInput)도 수용하도록 정규화 로직과 테스트를 추가해 500 오류를 방지.
2026-06-16 STEP 15 완료 - 인앱 브라우저 호환성을 위해 프로그램 강제 클릭 다운로드를 실제 링크 다운로드 방식으로 변경하고 테스트 및 프로덕션 빌드 통과를 확인.
2026-06-16 STEP 16 완료 - 추가 확인 질문을 한 개 카드의 연속 리스트로 통합하고 모든 질문 답변 전에는 초안 확정을 막는 검증 흐름을 구현.
2026-06-16 STEP 17 완료 - 상단 의미 없는 메뉴와 보조 히어로 카드를 제거하고 메인 히어로를 넓고 낮게 재구성했으며, 질문 카드를 스크롤형 저밀도 UI로 축소 조정.
2026-06-16 STEP 18 완료 - STEP 1 입력 영역을 단일 메인 열로 확장하고 엑셀 업로드/자연어 입력 카드를 더 넓은 가로 배치로 조정했으며 안내 카드는 하단 2열로 재배치.
2026-06-16 STEP 19 완료 - STEP 1 입력 카드 높이를 축소하고 하단 안내 카드 2개의 높이를 확대했으며 각 카드에 STEP 2 라벨을 추가.
2026-06-16 STEP 20 완료 - STEP 1 입력 중 긴 내용 때문에 가로 배치가 흔들리지 않도록 입력 카드 그리드 폭과 내부 줄바꿈 규칙을 유연하게 조정.
2026-06-16 STEP 21 완료 - 입력완료 후에도 첫 화면의 큰 메인 카드와 하단 2카드 골격을 유지하도록 STEP 2 검토/질문 레이아웃을 동일 구조 안으로 통합.
2026-06-16 STEP 22 완료 - 다중 가이드라인 엑셀 업로드, 통합 마스터 다운로드, 상품명 입력 기반 추출 다운로드 흐름을 화면 미리보기와 파일명 분기까지 반영.
2026-06-16 STEP 23 완료 - `Overview + Rule Master + Note Master + Rule-Note Map + Change Log` 5시트 템플릿을 정식 기준으로 문서화하고 export 엑셀도 상품명·판매일자 포함 구조로 정렬.
2026-06-16 STEP 24 완료 - 상품명 입력값이 상품 추출 다운로드 엑셀의 `Overview` 및 각 상세 시트의 `상품명` 필드에 실제 반영되도록 연결.
2026-06-16 STEP 25 완료 - 통합 마스터 다운로드를 버튼 기반 blob 저장 방식으로 안정화하고, 상품명 검색 시 통합마스터 후보 리스트를 보여 선택 후 추출 다운로드할 수 있게 수정.
2026-06-16 STEP 26 완료 - 인앱 브라우저 다운로드 호환성을 위해 `fetch/blob` 방식 대신 `/api/draft-export` 직접 다운로드 방식으로 전환하고 회귀 테스트와 빌드를 재검증.
2026-06-16 STEP 27 완료 - `초안 확정` 무반응 원인이던 `/api/draft-summary` dev 서버 캐시 오류를 재시작으로 복구하고, 요약 생성 실패 시 검토 화면에 오류 메시지가 보이도록 예외 처리와 회귀 테스트를 추가.
2026-06-16 STEP 28 완료 - 인앱 브라우저가 파일 다운로드 요청을 보내지 않는 문제를 우회하기 위해 `/api/draft-export/save` 서버 저장 방식으로 변경하고, 저장 완료 파일명·경로를 화면에 표시하도록 수정.
2026-06-16 STEP 29 완료 - 변경내용 입력 카드 위에 `현재 가이드라인 업로드` 가로 카드가 추가되어, 현재 가이드라인 전체를 표준화된 템플릿으로 바꾸는 안내가 먼저 보이도록 조정.
2026-06-16 STEP 30 완료 - 파일을 특정 디렉토리에 저장하지 않고, 다운로드 버튼 클릭 시 브라우저가 XLSX를 바로 내려받아 열 수 있도록 직접 다운로드 방식으로 되돌림.
2026-06-16 STEP 31 완료 - `현재 가이드라인 업로드` 배너를 더 넓고 낮은 가로형 카드로 다듬어, 변경내용 입력 카드 위에 먼저 표준화 안내가 보이도록 강화.
2026-06-16 STEP 32 완료 - `STEP 0` 배너를 추가해 업로드된 여러 가이드라인 엑셀이 표준 템플릿을 거쳐 하나의 통합 마스터 파일로 합쳐지는 흐름을 먼저 안내하도록 수정.
2026-06-16 STEP 33 완료 - `STEP 0` 배너를 `STEP 1` 카드 밖으로 분리해, 변경내용 입력 카드 위에 독립된 가로 카드로 보이도록 위치를 바로잡음.
2026-06-16 STEP 34 완료 - `/preview` 라우트를 추가해 현재 메인 화면을 웹에서 바로 열어볼 수 있는 미리보기 진입점을 제공함.
2026-06-16 STEP 35 완료 - STEP 0 기준 파일 저장, STEP 1 변경 입력 분리, 저장본 기준 비교 강조가 들어간 전체 통합 마스터 워크플로우와 XLSX 다운로드를 구현함.
2026-06-16 STEP 36 진행 - STEP0 카드에 미리보기 버튼과 저장된 통합 마스터 테이블 미리보기 패널을 추가하고, 참고 디자인처럼 가로형 버튼 배치와 카드 톤을 더 다듬는 중.
2026-06-16 STEP 37 진행 - 미리보기 버튼을 상단 STEP0 버튼군에서 빼고 저장된 테이블 미리보기 카드 안으로 옮겨, 미리보기 흐름이 해당 카드에서 바로 시작되도록 조정 중.
2026-06-16 STEP 38 진행 - 상단 단계 진행 바를 `0 통합 마스터 파일 만들기 → 1 변경내용 입력 → 3 검토메모 및 질문 확인` 구조로 재정렬 중.
2026-06-16 STEP 39 완료 - `/preview` 라우트를 공통 홈 컴포넌트와 분리해 다시 연결하고, 동적 렌더로 고정해 HTML 미리보기 화면이 이전처럼 내려오도록 복구함.
2026-06-16 STEP 40 완료 - 상품 추출 다운로드가 한글 파일명에서 500으로 깨지던 문제를 `Content-Disposition` 안전 인코딩으로 수정해 다운로드가 실제로 시작되도록 복구함.
2026-06-16 STEP 41 완료 - 확인 질문과 검토메모를 기준파일/변경파일/직접입력 요약에 따라 동적으로 생성하도록 바꿔, 입력 내용마다 질문이 달라지게 반영함.
2026-06-16 STEP 42 완료 - 통합 마스터/상품 추출 다운로드를 내장 브라우저 호환 방식으로 바꾸고, `jsdom` 테스트 환경에서는 기존 클릭 fallback이 유지되도록 조정함.
2026-06-16 STEP 43 완료 - STEP0에서 여러 가이드라인 엑셀을 개별 표준화한 뒤 하나의 통합 마스터로 머지하는 흐름을 반영하고, 업로드 파일 수·처리 방식·머지 이력을 워크북 초안에 포함함.
2026-06-16 STEP 44 완료 - 검토 질문을 고정 문구가 아니라 통합 마스터/변경 입력 메모를 기준으로 생성하도록 맞춰, 확인 질문이 입력 내용에 따라 달라지게 정렬함.
2026-06-16 STEP 45 완료 - 통합 마스터/상품 추출 다운로드를 실제 브라우저에서 `fetch -> blob -> 파일 저장` 방식으로 안정화하고, 파일명 헤더를 읽어 저장명을 복원하도록 개선함.
2026-06-16 STEP 46 완료 - 다운로드 경로를 다시 직접 링크 기반으로 단순화해, 변경 후에도 통합 마스터/상품 추출 다운로드가 브라우저에서 바로 동작하도록 재안정화함.
2026-06-16 STEP 47 완료 - STEP 1부터 시작하는 세로형 단일 흐름으로 화면을 재배치하고, STEP 3 검토 카드와 STEP 4/5 다운로드 카드를 초안 생성 뒤 아래로 펼쳐지도록 연결함.
2026-06-16 STEP 48 완료 - STEP 1 카드의 버튼 배치를 세로형으로 바꾸고, 통합 마스터 생성 영역의 가로 비율과 미리보기 영역 폭을 넓혀 카드가 덜 답답하게 보이도록 조정함.
2026-06-16 STEP 49 완료 - STEP 1의 업로드/생성 버튼을 설명문 아래로 내려 세로 정렬하고, 버튼 두 개가 나란히 보이도록 배치해 카드 상단을 더 자연스럽게 정리함.
2026-06-16 STEP 50 완료 - 가이드라인 미선택 안내를 경고 아이콘이 포함된 배지 스타일로 바꿔 버튼 옆 경고 문구가 더 눈에 띄도록 조정함.
2026-06-16 STEP 51 완료 - 경고 문구를 버튼 옆의 더 작은 경고 배지로 옮겨, 버튼과 같은 행에서 높이를 절반 수준으로 줄여 보이도록 다듬음.
2026-06-16 STEP 52 완료 - 기준 파일 선택 시 경고 배지와 아이콘이 즉시 사라지고, 선택 파일 수만 표시되도록 숨김 조건을 테스트로 고정함.
2026-06-16 STEP 53 완료 - 미리보기 표가 카드 폭을 넘지 않도록 카드 자체를 고정하고, 표 영역에만 가로 스크롤이 생기게 조정함.
2026-06-16 STEP 54 완료 - 미리보기 화면은 기존 카드 크기를 유지하고, 표 영역에만 가로 스크롤이 생기도록 다시 고정함.
2026-06-16 STEP 55 완료 - 미리보기 카드 자체에 가로 스크롤을 열고, 미리보기 그리드 최소 폭을 늘려 스크롤바가 실제로 보이도록 보정함.
2026-06-16 STEP 56 완료 - 입력완료 뒤에 화면을 교체하지 않고, 동일 페이지 아래로 검토 메모와 질문 카드가 펼쳐지도록 전환 방식을 바꿈.
2026-06-16 STEP 57 완료 - 입력 단계와 검토 단계를 같은 세로 흐름으로 묶어 카드 사이 간격이 일정하게 보이도록 정리함.
2026-06-16 STEP 58 완료 - 검토 메모를 하나의 박스로 합치고, 조건이 있으면 확인 질문이 5개를 넘어서 추가되도록 확장함.
2026-06-16 STEP 59 완료 - STEP 4 결과 카드에 다시 흰색 카드 배경/테두리/그림자가 적용되도록 공통 카드 스타일에 포함함.
2026-06-16 STEP 60 완료 - STEP 1은 흰색 카드, 미리보기는 하늘색 카드로 분리하고 둘 다 STEP 2처럼 아이콘 배지를 추가함.
2026-06-16 STEP 61 완료 - 검토 메모 박스의 높이와 내부 여백을 줄이고, 상단 설명과의 간격만 조금 확보하도록 압축함.
2026-06-16 STEP 62 완료 - 초안 결과 카드에 `반영 요약`을 추가해, 답변 목록보다 먼저 무엇이 반영됐는지 한 줄로 보여주도록 개선함.
2026-06-16 STEP 63 완료 - Excel 기반 상품 후보 파서를 `xlsx`로 안정화하고, 판매일자를 후보 카드에 표시하며 미리보기 버튼 크기를 더 작게 조정함.
2026-06-16 STEP 64 완료 - STEP 1 라벨을 STEP 2처럼 캡슐형으로 단순화하고, 히어로 제목을 `신계약 인수기준 반영 Agent`로 변경하며 상하 여백을 조금 압축함.
2026-06-16 STEP 65 완료 - 실제 가이드라인 엑셀의 라벨 행과 `보험코드` 열을 읽어 상품코드·보험코드·판매일자를 통합 마스터 미리보기에 반영하도록 파서와 워크북 초안을 보정함.
2026-06-17 STEP 66 완료 - 묶음특약 하위 보장을 `파워수술 1종`처럼 부모 특약명과 결합해 보험코드 기준으로 행을 분리하고, 노트/맵/초안 표가 행 단위로 생성되도록 확장함.
2026-06-17 STEP 67 완료 - 병합 행의 `특약명`은 비워 두고, 상품별 특약 행만 보이도록 미리보기의 불필요한 통합 라벨을 제거함.
2026-06-17 STEP 68 완료 - `Change Log`도 통합 요약이 아니라 특약별 행으로 저장되도록 바꿔 변경 전후 이력이 행 단위로 남게 함.
2026-06-17 STEP 69 완료 - 미리보기 표의 `보험코드` 열이 긴 매핑 문자열 때문에 과도하게 늘어나지 않도록 해당 열만 줄바꿈/폭 제한을 적용함.
2026-06-17 STEP 70 완료 - 통합본 요약용 병합 행을 `Rule Master`/`Note Master`/`Rule-Note Map`에서 제거해 특약 단위 행만 남도록 정리함.
2026-06-17 STEP 71 완료 - 초기 통합 마스터는 `as-is`만 보이도록 하고, `to-be`는 변경내용 입력이 들어왔을 때만 생성되도록 초안 모드를 분리함.
2026-06-17 STEP 72 완료 - 업로드한 엑셀의 특약 행에서 `가입한도`와 `비고/수술유의`를 읽어 통합 마스터 초안에 그대로 반영하도록 파서와 표준화 출력을 확장함.
2026-06-17 STEP 73 완료 - `주석명`과 `주석 원문`에 특약명, 보험코드, 가입한도, 비고를 함께 넣어 주석이 비어 보이지 않도록 설명형으로 강화함.
2026-06-17 STEP 74 완료 - 오래된 저장본이 다시 읽히도록 마스터 하이드레이션 기준을 강화해 `가입한도`와 주석 텍스트가 빠진 예전 스냅샷을 자동 갱신하도록 함.
2026-06-17 STEP 75 완료 - 표의 `비고`와 표 아래 `주1)`류 본문주석을 모두 `Note Master`로 모아, 주석이 표 내부/외부에 섞여 있어도 같은 구조로 정리되도록 확장함.
2026-06-17 STEP 76 완료 - 저장본 복원 하이드레이터에도 `가입한도`, `비고`, 표 아래 `주1)` 본문주석을 다시 읽어오도록 맞춰서 Rule Master/Note Master가 비지 않게 복구함.
2026-06-17 STEP 77 완료 - `Note Master`를 공통 주석 레지스트리로 바꿔 동일 주석은 `Note ID`를 공유하고, `Rule Master`/`Rule-Note Map`/`Change Log`가 그 공통 노트를 참조하도록 정리함.
2026-06-17 STEP 77 완료 - 표의 `비고` 라벨을 표 아래 본문주석 텍스트로 역참조해 `주석 원문`에 실제 설명문이 들어가도록 보정하고, 계산식이 붙은 항목도 함께 보존함.
2026-06-17 STEP 78 완료 - `주2,3`처럼 한 셀에 섞인 주석 참조를 분리해 각 주석을 원자 단위로 `Note Master`에 쌓고, 동일 원문은 중복 제거되도록 노트 분해 로직을 보정함.
2026-06-17 STEP 79 완료 - 확인질문 생성이 `주2` 번호가 아니라 업로드된 특약/주석 연결 데이터를 기준으로 나오도록 바꾸고, 공통 주석·계산식·예외 주석에 대한 영향 확인을 우선 묻도록 정리함.
2026-06-17 STEP 80 완료 - 검토 질문이 노트 후보 일부만 보지 않도록 전체 영향 주석을 모두 질문으로 생성하고, 관련 특약/상품이 여러 개면 함께 반영 여부를 묻도록 확장함.
