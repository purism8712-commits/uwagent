# Common Core Flow Manual Check

- 브라우저에서 로컬 앱을 연다
- 히어로 제목이 `신계약 공통 에이전트`인지 확인한다
- `변경내용 입력` 화면에 `엑셀 업로드`와 `표 또는 자연어 직접 입력`이 모두 보이는지 확인한다
- `입력완료` 버튼을 클릭한다
- 2단계 화면에서 `검토메모`와 `확인 질문`이 보이는지 확인한다
- 질문 입력칸에 답변을 입력할 수 있는지 확인한다
- 데스크톱 폭에서 카드 레이아웃이 무너지지 않는지 확인한다
